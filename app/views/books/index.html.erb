<h1>Your Books</h1>

<%= link_to "Add New Book", new_book_path, class: "btn btn-primary" %>
<%= Book.count %>

<ul>
  <% @books.each do |book| %>
     <li>
      <%= link_to book.title, book %>
    </li>
  <% end %>
</ul>

